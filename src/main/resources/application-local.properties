spring.web.resources.static-locations=file:src/main/resources/static/
spring.web.resources.cache.period=0
spring.web.resources.chain.cache=false

spring.thymeleaf.cache=false
spring.thymeleaf.prefix=file:src/main/resources/templates/

spring.devtools.restart.enabled=true

# Local development: enable TOTP with a fixed test secret so 2FA can be exercised.
# Production keeps using the ADMIN_TOTP_SECRET environment variable (empty by default).
app.admin.totp-secret=${ADMIN_TOTP_SECRET:JBSWY3DPEHPK3PXP}
